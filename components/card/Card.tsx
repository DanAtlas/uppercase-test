import React from 'react';
import styles from './Card.module.scss';

const Card = () => {
  return (
    <div className={styles['card']}>
      <p>card text</p>
    </div>
  );
}

export default Card;
